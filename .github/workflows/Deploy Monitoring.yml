name: Deploy Monitoring Stack

on:
  push:
    branches:
      - groupTest
      - main
    paths:
      - 'k8s/monitoring/**'
      - '.github/workflows/Deploy Monitoring.yml'
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  deploy-monitoring:
    name: Deploy kube-prometheus-stack to AKS
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Get AKS credentials
        run: az aks get-credentials --resource-group rg-group1-austriaeast --name grp1-aks --admin --overwrite-existing

      - name: Setup Helm
        uses: azure/setup-helm@v3
        with:
          version: v3.12.3

      - name: Deploy kube-prometheus-stack via Helm
        run: |
          kubectl create namespace monitoring --dry-run=client -o yaml | kubectl apply -f -
          helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
          helm repo update
          helm upgrade --install kube-prometheus-stack prometheus-community/kube-prometheus-stack \
            -n monitoring \
            -f k8s/monitoring/helm-values/kube-prometheus-values.yaml \
            --wait

      - name: Apply monitoring manifests
        run: kubectl apply -f k8s/monitoring/manifests/

      - name: Verify Prometheus pods
        run: |
          kubectl get pods -n monitoring
          kubectl rollout status deploy/kube-prometheus-stack-grafana -n monitoring --timeout=180s
